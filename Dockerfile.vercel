FROM dunglas/frankenphp:1-php8.4

WORKDIR /app

RUN apt-get update && apt-get install -y zip unzip \
    && install-php-extensions zip pdo_mysql

COPY --from=composer:2 /usr/bin/composer /usr/bin/composer

COPY . .

RUN composer install --no-dev --optimize-autoloader

# Menyiapkan folder storage dan permission
RUN mkdir -p storage/framework/views storage/framework/sessions storage/framework/cache storage/logs \
    && chmod -R 777 storage bootstrap/cache

# Menggunakan sh/bash agar variabel $PORT dari Vercel terbaca secara dinamis
CMD ["sh", "-c", "php-server --listen :${PORT:-8000} --root public/"]